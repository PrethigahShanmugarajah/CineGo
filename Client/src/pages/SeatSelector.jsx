// CineGo / Client / src / components / SeatSelector.jsx

import SeatSelectorPage from "../components/SeatSelectorPage";

const SeatSelector = () => {
  return (
    <div>
      <SeatSelectorPage />
    </div>
  );
};

export default SeatSelector;

// CineGo / Client / src / pages / SeatSelector / SeatSelector.jsx
import SeatSelectorPage from "../../components/SeatSelectorPage/SeatSelectorPage";

const SeatSelector = () => {
  return (
    <div>
      <SeatSelectorPage />
    </div>
  );
};

export default SeatSelector;
